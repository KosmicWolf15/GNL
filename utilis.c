// tester de GNL: /* https://github.com/Tripouille/gnlTester */

// Nos piden una función que devuelve una linea leida de un FD

// No puedo usar LIBFT

// Debe compilar con la flag -D BUFFER_SIZE=XX 
// Esta flag se usa para determinar el tamaño del buffer de las lecturas del GNL

// Compilaremos de la siguiente forma cc -Wall -Wextra -Werror -D BUFFER_SIZE=42 <archivos>.c.

//El read debera utilizar el BUFFER_SIZE definido durante la compilación

// FILE DESCRIPTOR: Numero que identifica de forma unica un archivo abierto
//					en el sistema operativo.
// VARIABLE STATIC: Se usa para declarar la variable que mantendra su valor
//					en la memoria entre llamadas a la función.
